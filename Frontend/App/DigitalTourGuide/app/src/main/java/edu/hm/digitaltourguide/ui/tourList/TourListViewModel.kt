package edu.hm.digitaltourguide.ui.tourList

import androidx.lifecycle.ViewModel

class TourListViewModel() : ViewModel() {

    //val readAllRecipes: LiveData<List<Route>>
    //private val repository: RecipeRepository = RecipeRepository(database)

    init {
        //readAllRecipes = repository.readAllRecipes
    }
}